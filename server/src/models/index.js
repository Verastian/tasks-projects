const Task = require('./task.model')
const Column = require('./column.model')
const Project = require('./project.model')
const User = require('./user.model')

module.exports = { Task, Column, Project, User }
